---
phase: 02-console-immersion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/index.css
  - src/App.jsx
  - src/components/Hero.jsx
  - src/components/Navbar.jsx
  - src/data.js
autonomous: true
requirements: [IMM-01, IMM-02, IMM-05]

must_haves:
  truths:
    - "A subtle scanline texture is visible over the entire viewport in BOTH light and dark themes, and never intercepts clicks or hovers (IMM-01)"
    - "With `prefers-reduced-motion: reduce` active, the scanline overlay is absent, the hero boot line shows its full text immediately with no caret, and the status pill is visible (IMM-01, IMM-02)"
    - "On page load the hero eyebrow boot line reveals itself character-by-character with a blinking caret, then the RUNNING status pill appears (IMM-02)"
    - "Hovering (or keyboard-focusing) a navbar link fades in a `$` prefix in accent colour without shifting the link's position (IMM-05)"
    - "Hovering the RL-01 process chip reveals extra process metadata inline, and the chip's link still announces as 'RL-01' to assistive tech"
  artifacts:
    - "src/index.css — `.crt`, `.hero__eyebrow-type`, `.navbar__brand` chip wrapper, `.navbar__brand-id`, `.navbar__brand-clock`, `.navbar__links a::before` rules plus CRT theme tokens in all four token blocks and new entries in the existing reduced-motion block"
    - "src/App.jsx — mounts the CRT overlay element as the first child"
    - "src/components/Hero.jsx — renders `profile.bootLine` inside the typing span"
    - "src/components/Navbar.jsx — brand chip restructured into a wrapper span containing the RL-01 link and an empty clock slot"
    - "src/data.js — `profile.bootLine` and `profile.processMeta` exported"
  key_links:
    - "The existing `@media (prefers-reduced-motion: reduce)` block in src/index.css MUST gain explicit `.crt` and `.hero__eyebrow-type` rules — the generic `* { animation-duration: 0.001ms }` rule does NOT reset `animation-delay`, so without explicit `animation: none` overrides the boot line stays invisible for 2.6s and the caret freezes visible"
    - "CRT theme tokens must be declared in ALL FOUR token blocks (`:root`, `@media (prefers-color-scheme: dark) :root`, `:root[data-theme='dark']`, `:root[data-theme='light']`) — omitting the `[data-theme]` blocks silently breaks the manual toggle from Phase 1"
    - "The typewriter width animation reads `--type-chars` set inline from `profile.bootLine.length` — a mismatch clips the text or leaves dead whitespace before the caret"
    - "`.navbar__brand-clock` rendered empty here is the exact element plan 02-02 fills with `useClock` — the class name and DOM position are a contract between the two plans"
---

<objective>
Apply the console/terminal micro-interaction layer from Sketch A to the live site: a CRT scanline overlay, a typewriter boot line in the hero eyebrow, console-style `$` hover affordances on the nav, and a hover-reveal process chip that is pre-structured to host the live clock.

Purpose: Delivers IMM-01, IMM-02 and IMM-05 — the pure-presentation half of Phase 2. Everything here is CSS-driven so it is covered by the site's global reduced-motion contract, and it lands the DOM structure that plan 02-02's live data hangs off, avoiding rework.

Output: Console aesthetic visible site-wide in both themes, with zero new dependencies.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-console-immersion/02-CONTEXT.md
@.planning/sketches/01-console-micro-interactions.html
@CLAUDE.md
@src/index.css
@src/App.jsx
@src/components/Hero.jsx
@src/components/Navbar.jsx
@src/data.js
</context>

<interface_context>
Current shape of the code this plan modifies (verified 2026-07-30):

- `src/index.css` token blocks live at lines 1-20 (`:root`, light defaults), 21-36 (`@media (prefers-color-scheme: dark)`), 38-51 (`:root[data-theme='dark']`), 53-66 (`:root[data-theme='light']`). Every colour is a `--color-*` custom property; `--ease: cubic-bezier(0.16, 1, 0.3, 1)` is the shared easing token.
- The reduced-motion block is `@media (prefers-reduced-motion: reduce)` at lines 174-188. It currently resets `html { scroll-behavior }`, `.reveal`, and a global `* { animation-duration: 0.001ms !important; animation-iteration-count: 1 !important; transition-duration: 0.001ms !important; }`. It does NOT touch `animation-delay`.
- `.navbar` has `z-index: 20`. `.hero__blob` has `z-index: -1`. No other stacking values exist in the file.
- The responsive nav breakpoint is `@media (max-width: 720px)` (line 868); `.navbar__status` is `display: none` there and `.navbar__links` becomes a fixed full-screen drawer. There is a separate `@media (max-width: 860px)` (line 427) for the hero grid only.
- `.navbar__brand` (line 224) is currently styled as the bordered accent chip and is applied directly to an `<a href="#top">RL-01</a>` in `src/components/Navbar.jsx` line 63. `.navbar__brand:hover` sets `background: var(--color-accent-soft)`.
- `.navbar__links a` (line 245) is already `position: relative` and owns an `::after` pseudo-element used for the underline. The `$` affordance therefore needs `::before`.
- `.hero__eyebrow` (line 454) is `display: flex; align-items: center; gap: 0.5rem; flex-wrap: wrap;` in `var(--font-mono)`. It inherits the `rise` entrance animation via `.hero__content > *` (line 437) with `animation-delay: 0.05s` (line 443) — that animation is on the `<p>` itself, so child animations do not conflict.
- `src/components/Hero.jsx` line 13-15 renders the eyebrow as the literal text `PROCESS // ` followed by `<span className="status-pill status-pill--run">● RUNNING</span>`.
- `src/data.js` exports a `profile` object (name, initials, role, tagline, location, email, github, linkedin, resumeFile). Per CLAUDE.md, all site copy belongs here.
- JSX attribute style in this repo is double quotes (`className="navbar__inner"`); JS strings are single-quoted with no semicolons.
</interface_context>

<tasks>

<task type="tracer">
  <name>Task 1: CRT scanline overlay, end to end (IMM-01)</name>
  <files>src/index.css, src/App.jsx</files>
  <action>
Wire ONE console-immersion effect through every layer this phase touches — theme token block, component rule, React mount point, and the reduced-motion gate — so the whole vertical is proven before any further console styling is built on it.

In `src/index.css`, add three CRT tokens to ALL FOUR existing token blocks (`:root`, the `@media (prefers-color-scheme: dark)` `:root`, `:root[data-theme='dark']`, `:root[data-theme='light']`). Light-theme values: `--crt-line: rgba(0, 0, 0, 0.035)`, `--crt-blend: multiply`, `--crt-opacity: 0.45`. Dark-theme values: `--crt-line: rgba(255, 255, 255, 0.025)`, `--crt-blend: overlay`, `--crt-opacity: 0.5`. The light values differ deliberately: Sketch A only mocked dark theme, and its white lines with `overlay` blend are invisible on the light `#f3f5f8` background — dark lines with `multiply` are the light-theme equivalent (Claude's Discretion per CONTEXT.md "Scanline opacity/blend fine-tuning").

Add a `.crt` rule starting at column 0 (so the verify gate's block extraction works). Follow Sketch A lines 50-64: `position: fixed; inset: 0; pointer-events: none; z-index: 50;` with a `repeating-linear-gradient(to bottom, var(--crt-line) 0px, var(--crt-line) 1px, transparent 1px, transparent 3px)` background, `mix-blend-mode: var(--crt-blend)` and `opacity: var(--crt-opacity)`. `z-index: 50` sits above the navbar's 20 so the texture covers the whole chrome; `pointer-events: none` is what keeps the navbar, links and theme switch clickable underneath.

Extend the EXISTING `@media (prefers-reduced-motion: reduce)` block (do not create a second one) with `.crt { display: none; }`. CONTEXT.md and IMM-01 both require the overlay to be disabled entirely under reduced motion, not merely slowed.

In `src/App.jsx`, mount the overlay as the first child of the returned fragment: a self-closing `div` with `className="crt"` and `aria-hidden="true"`. It is purely decorative and must not be exposed to assistive tech.
  </action>
  <verify>
    <automated>npm run lint &amp;&amp; npm run build &amp;&amp; sed -n '/^\.crt {/,/^}/p' src/index.css | grep -q 'pointer-events: none' &amp;&amp; sed -n '/^\.crt {/,/^}/p' src/index.css | grep -q 'var(--crt-blend)' &amp;&amp; sed -n '/@media (prefers-reduced-motion: reduce)/,/^}/p' src/index.css | grep -q '\.crt' &amp;&amp; grep -q 'className="crt"' src/App.jsx &amp;&amp; test "$(grep -c -- '--crt-line' src/index.css)" -ge 4</automated>
  </verify>
  <done>`npm run build` succeeds; the `.crt` rule is `pointer-events: none` and theme-token driven; `--crt-line` is declared in exactly 4 places (one per token block); the reduced-motion block references `.crt`; App.jsx mounts the overlay.</done>
</task>

<task type="auto">
  <name>Task 2: Typewriter boot line in the hero eyebrow (IMM-02)</name>
  <files>src/data.js, src/components/Hero.jsx, src/index.css</files>
  <action>
In `src/data.js`, add `bootLine: '$ whoami → Senior RPA Engineer'` to the `profile` object. The copy belongs in data.js, not the component, per CLAUDE.md "Content lives in one place". Keep it short — it renders on one line and is width-animated in `ch` units.

In `src/components/Hero.jsx`, replace the literal `PROCESS // ` text in `.hero__eyebrow` with a span carrying `className="hero__eyebrow-type"` and an inline style object setting the custom property `--type-chars` to `profile.bootLine.length`, rendering `{profile.bootLine}` as its child. Leave the existing `status-pill status-pill--run` span as the following sibling, untouched.

In `src/index.css`, add the typing rules after `.hero__eyebrow`. Follow Sketch A lines 93-102 but drive the width from the custom property instead of a hard-coded `38ch`: `.hero__eyebrow-type` gets `overflow: hidden; white-space: nowrap; max-width: 100%; width: 0; border-right: 2px solid var(--color-accent);` and `animation: type 2.2s steps(var(--type-chars), end) 0.3s forwards, caret 0.9s step-end infinite;`. Define `@keyframes type { to { width: calc(var(--type-chars) * 1ch); } }` and `@keyframes caret { 50% { border-color: transparent; } }`. The `ch` unit is accurate here because `.hero__eyebrow` already inherits `var(--font-mono)`. `max-width: 100%` keeps the nowrap line from overflowing narrow viewports.

Make the status pill arrive after the line finishes typing so the eyebrow reads as a boot sequence: `.hero__eyebrow .status-pill { opacity: 0; animation: fade-in 0.4s var(--ease) 2.6s forwards; }` with `@keyframes fade-in { to { opacity: 1; } }`.

Then extend the EXISTING reduced-motion block with explicit overrides — this is the load-bearing part. Add `.hero__eyebrow-type { animation: none; width: auto; border-right: none; }` and `.hero__eyebrow .status-pill { animation: none; opacity: 1; }`. The block's generic `* { animation-duration: 0.001ms !important }` rule collapses duration but leaves `animation-delay` intact, so without these overrides a reduced-motion visitor sees an empty eyebrow for 0.3s and a missing status pill for 2.6s, and the caret border freezes mid-blink.
  </action>
  <verify>
    <automated>npm run lint &amp;&amp; npm run build &amp;&amp; grep -q 'bootLine' src/data.js &amp;&amp; grep -q 'hero__eyebrow-type' src/components/Hero.jsx &amp;&amp; grep -q -- "--type-chars" src/components/Hero.jsx &amp;&amp; sed -n '/@media (prefers-reduced-motion: reduce)/,/^}/p' src/index.css | grep -q 'hero__eyebrow-type' &amp;&amp; sed -n '/@media (prefers-reduced-motion: reduce)/,/^}/p' src/index.css | grep -q 'status-pill'</automated>
  </verify>
  <done>Boot line copy lives in data.js; Hero renders it through the typing span with `--type-chars` set inline; both the typing span and the status pill have explicit `animation: none` overrides inside the reduced-motion block; build and lint pass.</done>
</task>

<task type="auto">
  <name>Task 3: Console `$` hover affordances and hover-reveal process chip (IMM-05)</name>
  <files>src/data.js, src/components/Navbar.jsx, src/index.css</files>
  <action>
Nav `$` affordance (IMM-05, Sketch A lines 154-172). In `src/index.css` add `.navbar__links a::before` with `content: '$'; position: absolute; left: 0.1rem; color: var(--color-accent); opacity: 0; transition: opacity 0.2s var(--ease);` and reveal it on `.navbar__links a:hover::before, .navbar__links a:focus-visible::before, .navbar__links a.is-active::before { opacity: 1; }`. `.navbar__links a` is already `position: relative`, so the marker anchors to it. Absolute positioning is deliberate: Sketch A's list shifts `padding-left` on hover, which would reflow the navbar on every pointer move — here the marker occupies no flow space and only fades. Reveal on `:focus-visible` as well as `:hover` so keyboard users get the same affordance.

Sketch A's `.consolelist` pairs the `$` marker with a left border that shifts to accent on hover. Do NOT add a left border to the nav links: that half of the pattern is already satisfied here by the existing `.navbar__links a::after` underline, which scales in and is already `background: var(--color-accent)`. Adding a second accent affordance would double up. The `$` marker is the missing half. Inside the existing `@media (max-width: 720px)` block, add `.navbar__links a::before { display: none; }` — the mobile drawer is a different layout and the absolute marker would collide with its padding.

Process chip restructure. In `src/components/Navbar.jsx`, replace the single `<a className="navbar__brand">RL-01</a>` with a wrapper `<span className="navbar__brand">` containing (a) `<a className="navbar__brand-id" href="#top" data-hover={profile.processMeta} onClick={...}>RL-01</a>` keeping the existing `handleNavClick(e, '#top')` handler, and (b) `<span className="navbar__brand-clock" />` rendered empty. Import `profile` from `../data`. The clock span is intentionally empty here: it is the mount point plan 02-02 fills with the live `useClock` value, and its class name plus DOM position are a contract between the two plans. Moving the clock OUT of the anchor is required for accessibility — a ticking text node inside the link would rewrite the link's accessible name every second.

In `src/data.js` add `processMeta: 'uptime: 2y 4m'` to `profile`.

In `src/index.css`, split the current `.navbar__brand` rule: `.navbar__brand` keeps the chip shell (border, radius, padding, `font-family: var(--font-mono)`) and gains `display: inline-flex; align-items: center; gap: 0.6rem;` per Sketch A's `.chip` (lines 109-133); `.navbar__brand-id` takes the accent colour, weight and letter-spacing. Replace `.navbar__brand:hover` with a rule that keeps `background: var(--color-accent-soft)` on the wrapper and add `.navbar__brand:hover .navbar__brand-id::after, .navbar__brand-id:focus-visible::after { content: attr(data-hover); margin-left: 0.5rem; color: var(--color-text-muted); font-weight: 400; }` following Sketch A lines 127-132. Add `.navbar__brand-clock { color: var(--color-text-muted); font-weight: 400; font-size: 0.8rem; }`.

Inside the existing `@media (max-width: 720px)` block, hide both the hover reveal and the clock slot (`.navbar__brand-clock { display: none; }` and a `content: ''` / `margin: 0` reset on the hover `::after`), matching how `.navbar__status` is already hidden there. The mobile navbar has no room for chip expansion and the reveal would push the hamburger off-screen.

Verify the accent-on-`--color-accent-soft` chip text stays legible in the light theme, where `--color-accent` is `#c97b12` on a `rgba(201, 123, 18, 0.12)` wash — do not substitute a hard-coded colour, adjust via existing tokens only.
  </action>
  <verify>
    <automated>npm run lint &amp;&amp; npm run build &amp;&amp; grep -q 'processMeta' src/data.js &amp;&amp; grep -q 'navbar__brand-id' src/components/Navbar.jsx &amp;&amp; grep -q 'navbar__brand-clock' src/components/Navbar.jsx &amp;&amp; grep -q 'attr(data-hover)' src/index.css &amp;&amp; sed -n '/@media (max-width: 720px)/,/^}/p' src/index.css | grep -q 'navbar__brand-clock' &amp;&amp; grep -q "navbar__links a::before" src/index.css</automated>
  </verify>
  <done>Nav links reveal a `$` marker on hover and focus with no layout shift; the brand chip is a wrapper span holding the RL-01 link plus an empty `.navbar__brand-clock` slot; `data-hover` metadata comes from data.js and renders via `attr()`; both extras are suppressed at the 720px breakpoint; build and lint pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build → browser DOM | All content in this plan originates from `src/data.js`, authored in-repo. No untrusted input crosses any boundary in this plan. |
| viewport overlay → user input | The fixed full-viewport `.crt` element sits above all interactive chrome and can swallow every click if mis-specified. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Denial of Service | `.crt` fixed overlay in src/index.css | high | mitigate | `pointer-events: none` is mandatory on `.crt` and is asserted by the Task 1 automated gate (block-scoped `sed` extraction, so a stray match elsewhere in the file cannot satisfy it) |
| T-02-02 | Tampering | `attr(data-hover)` on `.navbar__brand-id` | low | accept | CSS `attr()` renders as text only and cannot execute; the value originates from in-repo `src/data.js`, not user input |
| T-02-03 | Information Disclosure | `profile.processMeta` / `profile.bootLine` copy | low | accept | Public résumé content deliberately published on a public site; no secrets are introduced |
| T-02-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package installs occur in this plan — the phase adds zero runtime dependencies. The Task 1 verify runs the existing toolchain only; a dependency-set assertion is enforced in plan 02-02 where new code is introduced. Package Legitimacy Gate is N/A (no install tasks). |
</threat_model>

<verification>
1. `npm run lint` exits 0.
2. `npm run build` exits 0.
3. `npm run dev`, then in the browser: page is covered by a faint scanline texture; every navbar control (links, theme switch, hamburger) is still clickable.
4. Reload with DevTools → Rendering → "Emulate CSS prefers-reduced-motion: reduce": the scanline is gone, the boot line is fully visible with no caret, and the RUNNING pill is visible immediately.
5. Toggle the Phase 1 theme switch: scanline remains visible and subtle in BOTH themes (dark lines on light, light lines on dark), and the chip/nav accents stay legible.
6. Hover each nav link: a `$` fades in and the link text does not move. Tab through the nav with the keyboard: the same `$` appears on focus.
7. Hover the RL-01 chip: `uptime: 2y 4m` appears inline. Resize below 720px: chip extras and the `$` markers are suppressed and the hamburger menu still works.
</verification>

<success_criteria>
- IMM-01 satisfied: scanline overlay visible site-wide in both themes, absent under `prefers-reduced-motion: reduce`, never blocks pointer input.
- IMM-02 satisfied: hero boot line types out on load with a blinking caret; full text shown instantly under reduced motion.
- IMM-05 satisfied: nav links show console-style `$` affordances on hover and focus.
- The RL-01 chip is restructured with an empty `.navbar__brand-clock` slot ready for plan 02-02.
- `package.json` dependencies are unchanged (react, react-dom only).
- All new motion is CSS-driven and explicitly covered by the existing reduced-motion block.
</success_criteria>

<output>
Create `.planning/phases/02-console-immersion/02-01-SUMMARY.md` when done
</output>
